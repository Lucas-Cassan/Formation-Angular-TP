import { ColorDisplayDirective } from './color-display.directive';

describe('ColorDisplayDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorDisplayDirective();
    expect(directive).toBeTruthy();
  });
});
